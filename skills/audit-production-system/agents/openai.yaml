interface:
  display_name: "Аудит производственной системы"
  short_description: "Проверяет модель, развитие и BPMN-сборку"
  default_prompt: "Используй $audit-production-system, чтобы провести доказательный read-only аудит точной версии, snapshot, реестров развития и BPMN/SVG-сборки без изменений."
